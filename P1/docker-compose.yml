version: "3.9"

services:
  image1:
    build:
      context: ./image1
      dockerfile: Dockerfile
    container_name: image1
    tty: true
    stdin_open: true

  image2:
    build:
      context: ./image2
      dockerfile: Dockerfile
    container_name: image2
    tty: true
    stdin_open: true